 - cachix mlabs key
 - git credentials
 - nixconf
 - micro (or else)
 - modern coreutils
 - gnome:
   * workspace shortcuts
 - flake init snippet 
 - vscode:
   * ctrl+p on selection
 - shell/zsh:
   * [x] history search based on whats written already
   * [ ] zawijanie linii komend w shellu dla długich linii
